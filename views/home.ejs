<!DOCTYPE html>
<html lang="en">

<head>
    <%- include('./partials/head.ejs') %> 
    <title>Welcome | Auth System</title>
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: #ffffff;
            margin: 0;
            padding: 0;
            color: #333;
        }

        /* Navbar (assuming nav.ejs adds a nav element) */
        nav {
            background: #000;
            padding: 15px 30px;
        }

        nav a {
            color: #fff;
            margin-right: 20px;
            text-decoration: none;
            font-weight: 500;
        }

        nav a:hover {
            text-decoration: underline;
        }

        header {
            padding: 50px 20px;
            text-align: center;
            background: #f4f4f4;
            color: #000;
        }

        .container {
            max-width: 900px;
            margin: 40px auto;
            background: #fff;
            padding: 30px;
            border: 1px solid #e0e0e0;
            border-radius: 8px;
        }

        h1, h2 {
            color: #000;
        }

        .info {
            margin-bottom: 15px;
            line-height: 1.6;
            color: #555;
        }

        .cta-buttons {
            margin-top: 25px;
        }

        .cta-buttons a {
            text-decoration: none;
            background: #000;
            color: white;
            padding: 12px 25px;
            border-radius: 6px;
            margin-right: 10px;
            transition: all 0.3s ease;
            display: inline-block;
        }

        .cta-buttons a:hover {
            background: #333;
        }

        footer {
            text-align: center;
            padding: 15px;
            color: #000;
            background: #f4f4f4;
            margin-top: 50px;
        }

        .user-debug {
            margin-top: 20px;
            background: #fafafa;
            padding: 10px;
            border: 1px solid #ddd;
            border-radius: 5px;
            font-size: 0.85rem;
            color: #555;
            overflow-wrap: break-word;
        }
    </style>
</head>

<body>
    <%- include('./partials/nav.ejs') %> 

    <header>
        <h1>Welcome to Our Auth System</h1>
        <p>Secure Sign In & Sign Up made simple and elegant.</p>
    </header>

    <div class="container">
        <h2>About This Platform</h2>
        <p class="info">
            Easily register a new account or sign in to your existing one. Our platform prioritizes security and a seamless user experience.
        </p>
        <p class="info">
            New here? Sign up in seconds. Already have an account? Sign in now.
        </p>

        <div class="cta-buttons">
            <a href="/user/signin">Sign In</a>
            <a href="/user/signup">Sign Up</a>
        </div>

        <div class="user-debug">
            <strong>Debug Info (User Object):</strong>
            <br />
            <%= JSON.stringify(user) %>
        </div>
    </div>

    <footer>
        &copy; 2025 Auth System | All rights reserved
    </footer>

    <%- include('./partials/script.ejs') %> 
</body>

</html>
